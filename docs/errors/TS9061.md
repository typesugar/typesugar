<!-- generated:begin — do not edit inside this block; regenerate with `node scripts/generate-error-docs.mjs` -->
<!-- prettier-ignore-start -->

# TS9061

**Category:** Macro Syntax · **Severity:** Error

## Message

```
Macro `{name}` is not defined
```

## Explanation

The macro `{name}` is called but not imported.

typesugar macros must be imported to be used:

  import { {name} } from "typesugar";

Common macros:
- comptime — compile-time evaluation
- match — pattern matching
- derive — auto-derive instances
- cfg — conditional compilation

<!-- prettier-ignore-end -->
<!-- generated:end -->
