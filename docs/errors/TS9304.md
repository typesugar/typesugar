<!-- generated:begin — do not edit inside this block; regenerate with `node scripts/generate-error-docs.mjs` -->
<!-- prettier-ignore-start -->

# TS9304

**Category:** Higher-Kinded Types · **Severity:** Error

## Message

```
@hkt must contain exactly one `_` placeholder, found {count}
```

## Explanation

The @hkt macro requires exactly one `_` marker in the type alias RHS.
Multiple `_` markers create ambiguity about which position is the HKT hole.

Correct:
  /** @hkt */
  type MapF&lt;K&gt; = Map&lt;K, _&gt;;   // One _ — K is fixed, _ is the hole

Incorrect:
  /** @hkt */
  type PairF = [_, _];        // Two _ — which is the hole?

For multi-arity type constructors, fix all but one parameter:
  /** @hkt */
  type MapF&lt;K&gt; = Map&lt;K, _&gt;;   // Fix K, vary the value type

<!-- prettier-ignore-end -->
<!-- generated:end -->
