<!-- generated:begin — do not edit inside this block; regenerate with `node scripts/generate-error-docs.mjs` -->
<!-- prettier-ignore-start -->

# TS9220

**Category:** Macro Syntax · **Severity:** Error

## Message

```
@tailrec: {reason}
```

## Explanation

The @tailrec decorator requires functions to be in tail-recursive form.

A tail-recursive call must be:
1. The last expression before returning
2. Not wrapped in other operations (like x + recurse(...))
3. Actually present (the function must call itself)

<!-- prettier-ignore-end -->
<!-- generated:end -->
