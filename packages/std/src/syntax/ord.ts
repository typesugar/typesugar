/**
 * Activation marker (PEP-052) — import this module to activate `Ord` METHOD
 * syntax in the importing file. Additive (introduces method names only).
 *
 * ```ts
 * import "@typesugar/std/syntax/ord";
 * ```
 *
 * For the comparison operators (`a < b`, `a <= b`, `a > b`, `a >= b`), import
 * `@typesugar/std/syntax/ord/ops`.
 *
 * @syntax-methods Ord
 */
export const __typesugar_syntax_methods_Ord = true;
