/**
 * Source Map Utilities — re-exported from @typesugar/transformer-core
 */
export {
  composeSourceMaps,
  decodeSourceMap,
  decodeMappings,
  findOriginalPosition,
  findGeneratedPosition,
  type DecodedSourceMap,
  type DecodedSegment,
  type DecodedLine,
  type SourcePosition,
} from "@typesugar/transformer-core";

export type { RawSourceMap } from "@typesugar/core";
