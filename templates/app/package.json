{
  "name": "typesugar-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "prepare": "ts-patch install -s"
  },
  "dependencies": {
    "typesugar": "^0.1.0",
    "@typesugar/sql": "^0.1.0"
  },
  "devDependencies": {
    "@typesugar/transformer": "^0.1.0",
    "ts-patch": "^3.3.0",
    "typescript": "^5.8.3",
    "unplugin-typesugar": "^0.1.0",
    "vite": "^6.0.0"
  }
}
